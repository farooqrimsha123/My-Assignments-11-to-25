// Names:Store the names of a few of your friends in a array called names. Print each person's name by accessing ach elements in the list, one at a time.
var members = ['rabia', 'faiza', 'rimsha', 'mehak', 'hina'];
for (var i = 0; i < members.length; i++) {
    console.log(members[i]);
}
